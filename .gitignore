/node_modules
/.next
/out
/build
.DS_Store
*.pem
.env*
!.env.example
/coverage
next-env.d.ts

# Internal-only strategy notes — never publish, never route.
/internal/

# cloudflare / opennext build output
.open-next
.wrangler

# Design canvas / brand kit — kept local, not shipped with the repo.
/design-system/

# TypeScript incremental build info
*.tsbuildinfo
